import MarketingLayout from "./(marketing)/layout";
import NotFoundComponent from "./(marketing)/not-found";

export default function NotFound() {
  return (
    <MarketingLayout>
      <NotFoundComponent />
    </MarketingLayout>
  );
}
